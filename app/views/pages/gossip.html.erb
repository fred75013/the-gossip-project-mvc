<div class="text-center mt-5 fs-3">
    <p>Titre du Gossip : <span class="text-secondary fs-4"><%=Gossip.find_by(title: @title).title%></span></p>
    <p>Contenu : <span class="text-secondary fs-4">"<%=Gossip.find_by(title: @title).content%>"</span></p>
    <p>Créer par : <span class="text-secondary fs-4"><%=Gossip.find_by(title: @title).user.first_name%> <%=Gossip.find_by(title: @title).user.last_name%></span></p>
    <p>Créer le : <span class="text-secondary fs-4"><%=Gossip.find_by(title: @title).created_at%></span></p>

    <p><a href="/author/<%=Gossip.find_by(title: @title).user.first_name%>" class="btn btn-outline-primary">voir la fiche de l'auteur</a> <a href="/welcome" class="btn btn-outline-primary">Retour à l'accueil</a></p>
</div>